import Button from "@material-ui/core/Button";

const Tile = (props) => {
    return (
        <div>
            <Button variant="outlined" color="primary">Tile</Button>
        </div>
    );
}

export default Tile;